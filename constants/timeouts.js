/**
 * Shared timeout constants (milliseconds).
 */
export const TIMEOUTS = Object.freeze({
  SHORT: 5000,
  MEDIUM: 15000,
  LONG: 30000,
  EXTRA_LONG: 60000,
  NETWORK_IDLE: 15000,
  RETRY_DELAY: 500,
  ANIMATION: 1000,
});
